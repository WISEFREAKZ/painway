name: physio_app
description: "A secure, high-performance physical therapy mobile application."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: 1.0.8
  flutter_local_notifications: 17.1.2 
  shared_preferences: 2.2.3
  url_launcher: 6.2.6
  app_links: 6.0.1
  supabase_flutter: 2.6.0 # Restored securely directly under root dependencies

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 4.0.0

flutter:
  uses-material-design: true
