<?xml version="1.0" encoding="utf-8"?>
<!-- Shown as the Android Window background before Flutter draws its
     first frame. Matches the app's calm-teal wellness palette so the
     splash-to-app transition feels seamless. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@android:color/white"/>
</layer-list>
