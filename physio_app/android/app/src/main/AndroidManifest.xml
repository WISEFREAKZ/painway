<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- PERMISSIONS
         Required for flutter_local_notifications to schedule, survive
         reboot, and reliably fire the water + stretch reminders. -->

    <!-- Android 13+ (API 33+): required to actually show a notification -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Lets scheduled alarms/reminders re-register after the phone restarts -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- Android 12+ (API 31+): required to schedule exact-time alarms
         (used for the daily stretch reminder and each water reminder slot) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>

    <!-- Haptic alert when the in-app countdown timer completes -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- Standard network access for Supabase queries and Image.network() media -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Keeps the notification alarm precise even if the device enters
         Doze/App Standby, matching AndroidScheduleMode.exactAllowWhileIdle
         used in notification_service.dart -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:label="Physio &amp; Mobility"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Shows a splash screen until Flutter draws its first frame -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Required by the Flutter tooling for release/debug builds -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>

        <!-- flutter_local_notifications: system receivers.
             These allow scheduled notifications to fire correctly and to
             be re-scheduled automatically after a device reboot. -->

        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false"/>

        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <service
            android:name="com.dexterous.flutterlocalnotifications.ForegroundService"
            android:exported="false"
            android:stopWithTask="false"/>

    </application>

    <!-- Required on Android 11+ (API 30+) so the plugin/app can query
         whether it's allowed to schedule exact alarms at runtime. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN"/>
        </intent>
    </queries>

</manifest>
